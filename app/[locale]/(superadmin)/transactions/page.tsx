"use client";

import { Container, Title } from "@mantine/core";

export default function TransactionListPage() {
  return (
    <Container size="lg" py="xl">
      <Title order={1}>Transaction List</Title>
    </Container>
  );
}