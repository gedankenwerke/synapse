export type Tenant = {
    id: string;
    ParentID: string;
    Name: string;
    CreatedAt: string;
    UpdatedAt: string;
}

// not implemented yet, but this is the expected response from the API when fetching tenants
// export type TenantsResponse = {
// }